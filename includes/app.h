#pragma once

#include "defines.h"

/*
typedef struct {
	u32 cell_w, cell_h;
	u32 world_w, world_h;
} iso_scene;

typedef struct {
	const char title[256];
	u32 width, height;
	iso_scene scenes[100];
} iso_game_instance;
*/

